# Tests

Unit tests per agent, integration tests for cross-agent handoffs (e.g. Health Agent breach →
Gate Agent escalation), and policy-as-code rule tests (one test per rule, both pass and fail cases).
