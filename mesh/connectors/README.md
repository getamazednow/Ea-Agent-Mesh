# Connectors

Integrations the mesh reads from: CMDB, APM, cloud cost management, vulnerability scanner,
end-of-life registry, IaC/pipeline metadata. One subdirectory per source system; keep credentials
out of this repo (use the platform's secret store, referenced by name only).
